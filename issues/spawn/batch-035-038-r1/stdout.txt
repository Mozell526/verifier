Architect r1 is on 035–038. Script rerun matched SHA-256 `0205668a62c18e516a12d973e3273988235a02b6caafaf9a9807341548d95367`. Spawn-id `770d8502492aedbc`.

**035 — real-problem.** Whole-query is a local proxy, not the right object. `whole_query_cover` is three dead gates (`1` field, `1` value, `value==query`). Frozen collision happens because mixed has 0 multi-field rows and set A has 0 multi-field empty-residual **identity** rows (only I129, and it is `no_identity_field`). Crispness makes it a floor, not an architecture.

**036 — real-problem.** Object-cover is the right total cut. Residual/`的` must not be peeled (next rule table). Never overlay NF is an authorization boundary, not a hole. Three spec locks still needed: span order (field order, first unused hit), Unicode whitespace vs the listed three, and `other` in the §6 table. I248 is load-bearing: `红莲` has surname shape and is not a catalog product; residual `保单` is what blocks the lift.

**037 — real-problem.** Score collision is expected and honest, not a rename. Extra reach is only `SYN-concat-name-product` and `SYN-two-ids`. 41/47 is not a KPI. Empty status-diff is weaker than the multi-field scan; the scan is what actually proves the extra cells have no frozen identity row.

**038 — escalate-to-project.** Host is still L1504–1508 vs 1A. Same live `searchClientName MATCH <整句>`: 匡西永/张伟 NF, 王坤林 F. No smaller in-scope write fixes the source. Merging `decide_object_cover` would violate the charter, 012, and the user’s “rule-skin” rejection. `git diff -- impl/projects/client_search/draft/judge.py` is empty.
