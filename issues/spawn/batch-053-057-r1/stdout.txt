Appended one Architect Response to each of 053–057. Prior verifier text is untouched. Independent `--probe` / programmatic rerun used; 12/16 LLM judges were not rerun.

| Issue | Verdict | Why |
|---|---|---|
| **053** | **real-problem** | Last wave really patched the imported class, not the exec’d instance. Probe still required. `source=llm` is not a purity badge: all 16 T1 rows carry `client_search.condition_compare`, and the official 评估规范 still precedes the principle in the compiled system prompt. |
| **054** | **real-problem** | Not a fluke. Both 共展 rows (`SYN-gongzhan`, `I650`) were `fulfilled`; 张伟/杨杰 were also `fulfilled`. Prompt-only Q1 did not consume the existing 1A surname ruler. That ruler is the locked standard, not a new type table. Do not restore last-word geometry. |
| **055** | **reasonable-design** | T1 reasons are “another thing remains” (保单 / 领取状态 / 姓名), not `value==query`. Geometric/programmatic mouths inherit on those three sentences. This is the intended Q2, not the contrast geometry. |
| **056** | **reasonable-design** | Reproduced 9/9 and 27/27 myself. That mouth is isomorphic to `value==query` + the name ruler. The T1 agent did **not** copy it (共展/昊轩/红莲 diverge), so 056 and 054 do not collapse. Keep it as a negative control; do not sell 27/27 as generalization. |
| **057** | **real-problem** | Q1 probe independently: 张伟 够撑住, 共展 没有该维已识别的姓, last-word still identity. Feeding the existing ruler as read-only evidence is consuming the standard, not last-word in new clothes — 红莲保单 still splits Q1 pass / Q2 fail. Persist T2 to its own dump so `--probe` cannot clobber it. |

`--probe` rewrote `issues/trace/simulate_judge_agent_memory.json` (script side effect). T1-16 dump was not rewritten. T2’s 11 returned rows were judged from the snapshot read in this spawn, not from the now-empty live placeholder.
