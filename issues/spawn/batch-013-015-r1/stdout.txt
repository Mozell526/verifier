Appended **Architect Response #1** to all three issue files. Spawn-id `e288ae27bc0d2d22`. Did not touch `spec/**` or `impl/**`.

Three **real-problem** verdicts, not the same paragraph:

**013 — export gap, not “protocol forgot capability.”**  
`fulfilled` is specified to answer one question. Authority already classifies 职责外 / 职责内能力缺失 / 职责内正常, but only as a hidden intermediate (`authority.md` L295–L298: prompt constraint, no schema field; `authority_gate.py` rewrites status). `blocking` / `required_capabilities` / `authority_tool_call_ids` / `inlive_boundary` do not publish a combinable product-support result. 013 must not smuggle a 5-value enum.

**014 — user’s grouping is too coarse; “去年默认 α” is not a protocol fact.**  
They share only the demand-axis ban on 尽力了→F. They do not share “尚未支持.” Format-out is a clean β shape (009). I rejected default-assigning 去年 to α: that needs Authority + independent material, not catalog `is_supported=false`. 称谓 stays unassigned (§4.4). Axis 2 does **not** have to be a full α/β/γ enum — the hard constraint is that β/γ must not eat the “定位内尚未支持” label.

**015 — A/B/4th-state/redefine-F are dead; E’s inventory is not minimum.**  
User question “new label or change `fulfilled.md`”: changing §2 or adding a fourth fulfilled value is illegal. The live family is an orthogonal layer that does not rewrite overall. Rejected: E’s 5-value table as “minimum,” bundling disclosure-quality as a third axis, treating `boundary_decision` as a reserved hook (`info-volume.md` L305 says cut it), and pretending 012’s program can emit β. Names / spec file / schema change remain §4.1 / §4.2 / §4.5.
