三个 issue 都已独立核查并追加 `Architect Response #1`，没有改原内容。

**010 — real-problem**  
当场重算新表：341 行、`Scenario=badcase` × 341、`fulfilled=213` / `not_fulfilled=128`。仓库里没有头部对照闸（只出现在 canvas 方案 E）。补一句：341 **不是**「零头部形态」（58 条数字 query 多数已是 F），缺的是独立集 B。集 B 从哪来仍归章程 §4。

**011 — real-problem**  
75 条 2–4 汉字裸词、其中 31 条 live `searchClientName`、F 只有杨杰/郑鑫/匡西永，与表一致。主证是 I336 匡西永 vs I539 王坤林：同一 `searchClientName MATCH`、同一 `疑似姓名候选、姓名-模糊匹配`，新 judge 一个咬「形态就是姓名检索」，一个咬「字段定义不算独立证据」。昊轩不是最干净的对子。共展 NF 必须保住。

**012 — real-problem**  
`ClientSearchJudge` 不选出口；`_unsupported_boundary_evidence` 只是材料。I046 把「去年」做成 blocking NF，I161 做成 non-blocking 仍 F。应对齐已有的 `_operator_justified`，把姓名 / blocking / 空间三道闸搬进代码；不要再往 L1477–1526 加句子。政策旋钮仍归章程 §4。

三份回复都带了 `spawn-id: 37225eeb6839ec61`、`pid: 45981`。本轮只读，没有改代码或 xlsx。
