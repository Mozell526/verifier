# Report — 第二问怎么实现、放在哪一层

用户问的是实现位置，不是再换一句口语：这个东西到底怎么算出来，应该放在哪个位置。现在前端只能看见 fulfilled。本轮不改判定，不改界面，不加字段。

官方对手 spawn-id `f7ab9fd0b34ddbc9`。闸：exit 0，隔离有效；`scope_valid=false` 全是 host 写的 context_store 旁路落盘，按惯例豁免。

两问结构不改。第一问已经有出口。第二问不是再开一张嘴，是读已经裁完的产品事实。

## 四层落点

| 层 | 落点 | 明确不是 |
|---|---|---|
| 协议 | 继续做 fulfilled 的兄妹文，不并进第一问 | 不是再发明第四种裁口 |
| 计算 | 判定写完第一问之后，读这一件已经写下的能力/职责裁口 | 不是判定再填一格；不是库存字段表；不是这次给没给到 |
| 存放 | 现在不进判定那张嘴。以后若存，只物化同一份读 | 不是 schema 新枚举；不是第二份真源 |
| 看见 | 以后若给人看：同一条期望上，办成了没有旁边，另开一格 | 不是案件表「状态」的第四种颜色；不是只在没办成行出现 |

现在只能看见第一问，是因为第二问还没有自己的格子。不是因为它只属于没办成。

规范格子在期望矩阵那一行上。那是唯一已经按第一问那一件分行的出口，不是人此刻停在主表「状态」上的视线。主表一行一个案子，放进去会把对象切粗。

## 怎么算

读已经写下的前缀，不新判一类：

```text
职责内正常         → 立住了
职责内能力缺失     → 没立住
职责外             → 说不清
还没裁 / 裁口打架 / 依据不够 → 说不清
```

这一次没裁过，就是说不清。这是出口，不是残缺。不能从办成了反推立住了，也不能去翻别的案子当初怎么裁的。

「立住了 / 没立住」继续只是内部手柄，不宣布采用为对外题面。

## Issues

| ID | 结论 | 一句话 |
|---|---|---|
| 058 | real-problem | 第二问是读，不是再开一张嘴；没裁口就是说不清，不准回看目录 |
| 059 | reasonable-design | 粒已经在同一条期望上；主表看不见，不能拿来切粗对象 |
| 060 | real-problem | 四层拆开；规范格子在矩阵 Status 旁边，不是「状态」芯片 |
| 061 | escalate-to-project | 位置已锁；打开那一格是项目决定，不是协议没做完 |

## 还没做、也不能由角色代做

看见 / 对外叫什么 / 现在改不改表，仍停住。本轮不替你打开那一格。
