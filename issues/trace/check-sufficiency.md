# Check list — 充分性轮（042–045）

范围：姓名判定的原则/内存对照，不是 8011 页面改动。
本轮不改生产代码。发现的非标准化先记录，修改前需用户确认。

| 项 | 结论 | 证据 |
|---|---|---|
| a 过度规则化 | 命中。对象覆盖用残句为空定义办成了没有；strip_any 用虚词表+放开字段类扩散 | 042 / 044；I129 综拓潜客、I246 客户 |
| b 局部样本修改 | 未做。没有点名 341 ID 当规则，没有改 xlsx | 新脚本只读冻结 live |
| c 只改结果不改源头 | 命中且停住。源头仍是 draft/judge.py 裸词规则与 1A 互搏 | 045；L1504–1508 |
| d 数据不一致 | 未引入新口径文件。旧脚本未改 | coverage / principle 脚本 SHA 未作为发版口径 |
| e 冗余代码 | 本轮不删旧对照函数。wide/surname/role/object_cover 留作负对照 | 044 |
| 协议对齐 | 充分性对齐 fulfilled.md §1 / §2.1，不引入第四态 | 043 |
| 泛化 | 离开姓名场不新开字段类；不改判是正式出口 | 042 / 044 合成针 |
| 优雅 | 不再同时维护覆盖门和充分性两套「原则」；覆盖门降对照 | 044 |
| 业务 | 只动 1A/4A；过严看王坤林；互有对错不拿 341 当唯一准星 | 043 消融保留 7 条；I595/I638 已从误抬名单拿掉；分数不作胜利 |
| 实测 | 内存脚本已跑通；未改 judge，故未重跑 48 次 LLM，也未测 8011 | dump `aaa5381cefef843ad52b706dc1a7813db5308c8b707f3ae07b3cefd859f5c980` |

未执行（需用户确认后才动）：

1. 改 `draft/judge.py` 裸词规则
2. 删 `exit_object_cover` / `exit_live_identity`
3. 重跑线上 judge
4. 动 8011 前端
