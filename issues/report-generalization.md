# Report — client_search judge 泛化：badcase 全集 vs 现实头部

本轮只立根因，不改 judge / prompt / xlsx / canvas。006–009 不重开。010–012 经 architect 独立挑战后全部 Consensus = real-problem。

## 互有对错 / 过严不是「新更严所以更好」

Canvas 自称：同 live 分歧里新更好 54、旧更好 9、互有对错 3。上一轮只审后两类（加 I046 对照），8 条已经钉死：问题出在提示内部几条指令互相打架，模型每次咬半句。

互有对错（表面）：

| ID | query | 旧→新 | 现在怎么看 |
|---|---|---|---|
| I485 | 昊轩 | F→NF | 两边都不干净。二字无姓，歧义真实。 |
| I539 | 王坤林 | F→NF | 比昊轩更过严。三字「姓+名」是头部形态。 |
| I161 | 林秀微去年投保全家保 | NF→F | 投保人+全家保读对了；「去年」与 I046 不同侧。 |
| I046 | 去年购买学业福 | F→NF | canvas 算「新更好」。同模式反判。 |

过严（表面）：

| ID | query | 旧→新 | 现在怎么看 |
|---|---|---|---|
| I263 | 小雨弟弟 | F→NF | 关系+性别已交付；「比本人小」空间里没有，却做成 blocking。 |
| I288 | 配 | F→NF | 对昊轩因裸词 NF，对「配」又要求必须抽出姓名。 |
| I616 | 海蜂老板娘周老板 | F→NF | 称谓不是自然人姓名。 |
| I034 | 大写P07。六个零。 | F→NF | 格式外。新 NF 错；旧 F 也对不上（拒绝≠办成）。 |
| I638 | C00OO731392 | F→NF | clientNo 已对，却用「收窄」打掉扩大。 |

这 8 条是观察项，不是发版 KPI，也不是泛化尺子。

## 这轮真正钉住的：为什么在 341 上改不好泛化

341/341 `Scenario=badcase`。新状态 F=213 / NF=128。仓库里没有冻结的头部对照集。canvas 方案 E 只是文案。

「平衡」如果做成下面任何一种，下一轮换一批长尾还会漂：

- 把 341 重加权、抽样，假装它是生产分布
- 从 341 里挑「看起来正常」当头部集
- 再往提示里加几条例外（昊轩算人名、去年不算核心……）

正确的平衡是**两道闸合取**，不是一个混合分数。

## 三层根因

| issue | 层 | 缺什么 | 关键证据 |
|---|---|---|---|
| 010 | 评测目标 | 只有 badcase 全集，没有头部 F 地板 | 341 全是 badcase；不是「表里完全没有头部形态」（约 58 条数字 query 多数已 F；姓名 F 只有杨杰/郑鑫/匡西永） |
| 011 | 同形态出口 | 同一 live 姓名，新 judge 左右互搏 | 最干净对子：I336 匡西永 F vs I539 王坤林 NF。字段、操作符、值形态、matched_patterns 四项相同 |
| 012 | 宿主 | 三道闸写在提示里，模型选半句 | `draft/judge.py` L1479–1525。不是消灭 LLM，是把出口和 blocking 标签搬进代码 |

006–009 是表面。只修表面会换一批长尾再漂。只加 prompt 例外，过拟合这张表。把 341 重加权，是用长尾假装生产分布。

## 泛化机制（可证伪，不点名 341 ID 当规格）

1. **双闸（过程闸，不是分数）**
   - 集 A = 本 xlsx：盘客/活动、假姓名（共展）、目录产品误走姓名，单向否决，不得回退。
   - 集 B = 独立头部：二字/三字中文姓名、姓名+产品、合法单号，F 不得掉。
   - 没有集 B 之前，禁止拿 341 准确率或过严 8 条发版。
2. **姓名出口进代码**
   - 先目录/枚举/地址/盘客，再看形态。
   - 杨杰与王坤林必须同侧。共展必须继续 NF。
   - 禁止再往括号里加「王坤林算人名」。
3. **blocking / 空间同样进代码**
   - 对齐已有 `_operator_justified` / fail-closed。
   - LLM 仍写证据；不得自行改出口标签。
   - 不要另起规则 DSL，不要再往 L1479–1525 加 A–D 句子。

## 你需要拍板

1. 2–4 字中文形态能否单独撑 F。无论选哪边，杨杰和王坤林必须同侧。
2. 「去年」是 blocking 核心还是附加说明。I046 与 I161 必须同侧。盘客/活动继续 NF。
3. 格式外/称谓空条件：NE 还是 non-blocking 说明。排除 F，也排除 NF。
4. 集 B 现造一截，还是等真实流量日志。

## 文件

- 章程 `issues/charter.md`（旧 8 条章程归档为 `issues/charter-split-overstrict.md`）
- issue 010–012（含 Architect Response + Consensus）
- 抽取 `issues/trace/generalization-discovery.md`、`issues/trace/name-class-table.json`
- spawn `issues/spawn/batch-010-012-r1/`（isolation_valid / scope_valid / spawn_id=`37225eeb6839ec61`）

未重跑 judge，未改代码。001–005 未动。006–009 不重开。

## 用户已拍（2026-08-15）

- **1A**：2–4 字中文名可单独撑 F；杨杰/王坤林同侧=F。
- **4A**：已造独立集 B，18 条，见 `impl/projects/client_search/draft/cases/head_set_b.json`。
- **2、3 先不动**。判定代码本轮未改。
