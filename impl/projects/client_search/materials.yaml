# 资料槽位声明：项目契约，由项目维护者随代码版本管理。
# 协议见 docs/materials-system-implementation.md；填充走前端资料管理页或 /api/material/upload。
slots:
  - slot_id: field_glossary
    title: 字段口径表
    description: |
      judge 判断字段映射与取值对错的全景依据：按字段聚合的名称、业务含义、
      支持的 operators、value_types、枚举值样例与单位。逐字段细节仍以
      authority 工具的按需查询为准，本表提供全景口径。
    required: true
    roles: [judge]
    fill: [upload]
