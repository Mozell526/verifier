"""policy_search 项目的 tools 实现（scaffold 生成，待填充）。

继承 ProjectTools（来自 impl.core.tools_protocol）。
必须实现的扩展点已生成 stub，含协议基类的语义契约 docstring。
按项目业务填充方法体后删除 NotImplementedError。
"""
from __future__ import annotations

from impl.core.tools_protocol import ProjectTools


class PolicySearchTools(ProjectTools):
    """首版不新增工具，直接引用业务仓库已有协议与 evaluator。"""

    pass
