# Implementation Standard

This project follows `impl/project_implementation_standard-template.md` through the typed `runtime.application`, `runtime.adapter`, `runtime.batch_persistence`, `verifier.judge.boundary`, `verifier.attribution.trace`, `verifier.presentation.frontend_view`, and `verifier.check_rules.evidence` fields.

The standard records the curl-envelope request, stringified HTTP output, per-case URL resolution, judge boundary, and skipped attribution.
