# verifier 运行时依赖（Python 3.11，版本取自当前 agno conda 环境实测）
agno==2.3.21
fastapi==0.115.5
uvicorn==0.32.1
openai==2.14.0
pydantic==2.10.3
PyYAML==6.0.2
json_repair==0.54.3
requests==2.32.5

# embedding.enabled=true 时必需（百炼 embedding）；设 EMBEDDING_ENABLED=false 可不装
dashscope==1.25.22

# 仅 checklist/UAT 浏览器检查使用；llm_probe 评测链路不需要
selenium==4.40.0
